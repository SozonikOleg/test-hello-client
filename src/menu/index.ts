/**
 * Compatibility re-export (test brief: headless menu in `src/menu/`).
 * Implementation lives in FSD slice `shared/ui/menu`.
 */
export { default, HeadlessMenu } from '@/shared/ui/menu'
export * from '@/shared/ui/menu'
